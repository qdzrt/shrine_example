<%= form_for @audio, html: { role: 'form', multipart: true }  do |f| %>
    <% if @audio.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@audio.errors.count, "error") %> prohibited this attachment from being saved:</h2>
          <ul>
            <% @audio.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <%= f.file_field :file, class: 'input-file audio file-loading' %>

    <%= f.submit '确定' %>
<% end %>
